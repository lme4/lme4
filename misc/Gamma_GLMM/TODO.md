* reorganize: move multistart explorations into a subdir?
* more investigation of bias?
* backporting to lme4pureR?
* GH #936 log-gaussian `sigma` bias: mechanism now precisely characterized -- glmer's moment-based phi (`deviance/n` at the PIRLS conditional mode) is missing a `q` (number of random-effect levels) degrees-of-freedom correction; `phi_glmmTMB/phi_glmer` matches the predicted `n/(n-q)` to ~1% across B=100 replicates on the single-grouping-factor Rail example (see README). ~~(b) figured out what `q` should mean for crossed/multiple random-effect terms~~ done for additive, intercept-only, crossed grouping factors: `q_eff = sum(q_k) - (K-1)` (K = number of crossed RE terms), confirmed to within ~0.03 of an integer across 8 designs. ~~confirmed this is gaussian-specific or general~~ done: re-ran the identical 8-design sweep under `Gamma(link="log")` (pipeline now takes `family` as a parameter rather than hardcoding gaussian) and got the same `q_eff` pattern to the same precision -- not gaussian-specific (see README).
  - ~~(a) actually implementing an `n-q_eff`-style correction in glmer~~ first pass done: `glmerControl(disp_dof_correction=TRUE)` (opt-in, default `FALSE`), wired through `mkGlmerDevfun()`/`mkdevfun()`/`glmerPwrssUpdate()`/`glmerLaplaceHandle()` down to `profilePhi()` in `external.cpp`. `qEff` is computed once in R (`computeQEff()`, `R/modular.R`) from `reTrms$Gp` -- not recoverable from `Lambdat`/`Lind` alone in C++ for the general case (a correlated multi-parameter term contributes several `Lind` values for what is structurally one term). Falls back to the old uncorrected `n` (with a `warning()`) for any RE term with more than one column (random slopes, correlated blocks) -- untested territory for the formula, see next item. Spot-checked on `sleepstudy` (Gamma, scalar RE): corrected/uncorrected sigma ratio 1.0547 vs. predicted `sqrt(n/(n-q))` = 1.0541 -- close. **Not yet done: full test-suite run for breakage (both real bugs and reference-value drift from the sigma/logLik change when the flag is on), regression tests for `disp_dof_correction`, and a NEWS item** -- this was a same-day implementation, stopped here to avoid rushing validation.
  - (b) nested (rather than crossed) grouping factors, and random slopes / correlated multi-term REs: extend `misc/Gamma_GLMM/paramsurvey_loggaussian/` (which currently only covers crossed scalar-intercept designs) to also simulate a random-slope model and a correlated RE (e.g. `nlme::corAR1`/`glmmTMB::ar1()`) to test the prediction that `q_eff = sum(q_k) - (K-1)` still holds regardless of RE structure, since `q_k` is counted on the *spherical* (always-independent) `u` scale, not the correlated one -- unconfirmed guess, not yet tested empirically
* update the glmer vignette appropriately
